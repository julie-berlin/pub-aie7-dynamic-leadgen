# Dynamic Survey API - Environment Variables Configuration
# Copy this file to .env and fill in your specific values

# =============================================================================
# DEPLOYMENT ENVIRONMENT
# =============================================================================
ENVIRONMENT=development
# Options: development, staging, production
# Controls security settings, logging, and performance optimizations

LOG_LEVEL=INFO  
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Controls application logging verbosity

LOG_FILE=
# Optional: Path to log file for persistent logging
# Example: /app/logs/app.log (leave empty for console logging only)

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
# Your Supabase project URL - found in Project Settings > API

SUPABASE_PUBLISHABLE_KEY=eyJ...
# Supabase publishable key - safe to use in frontend code

SUPABASE_SECRET_KEY=eyJ...
# Supabase secret key - KEEP SECRET, used for backend operations

# =============================================================================
# AI/LLM CONFIGURATION (Required)
# =============================================================================
OPENAI_API_KEY=sk-...
# OpenAI API key for GPT models used in survey logic
# Get from: https://platform.openai.com/api-keys

# =============================================================================
# SECURITY CONFIGURATION (Required for staging/production)
# =============================================================================
ADMIN_API_KEY=your-secure-admin-key-here
# Strong API key for admin endpoints (min 32 characters recommended)
# Generate with: openssl rand -base64 32

JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long
# Secret for JWT token signing (min 32 characters required)
# Generate with: openssl rand -base64 48

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================

# LangChain Tracing (Optional)
LANGCHAIN_API_KEY=
# For tracing AI operations - sign up at https://smith.langchain.com/
LANGCHAIN_PROJECT=dynamic-survey

# Tavily Search (Optional) 
TAVILY_API_KEY=
# For web search capabilities - sign up at https://tavily.com/

# Error Tracking (Production Recommended)
SENTRY_DSN=
# Sentry DSN for error tracking and monitoring
# Get from your Sentry project settings

# =============================================================================
# REDIS CONFIGURATION (Optional)
# =============================================================================
REDIS_URL=redis://localhost:6379
# Redis connection URL for caching and session storage
REDIS_PASSWORD=
# Redis password if authentication is enabled

# =============================================================================
# EMAIL CONFIGURATION (Optional)
# =============================================================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@your-domain.com
# Email settings for notifications and lead communications

# =============================================================================
# MONITORING & HEALTH CHECKS (Optional)
# =============================================================================
HEALTH_CHECK_INTERVAL=300
# Seconds between automatic health checks (default: 5 minutes)

METRICS_ENABLED=true
# Enable application metrics collection

# =============================================================================
# RATE LIMITING (Production Settings)
# =============================================================================
RATE_LIMIT_ENABLED=true
# Enable/disable rate limiting (automatically enabled in production)

RATE_LIMIT_PER_MINUTE=60
# Max requests per minute per IP (overrides config file if set)

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
CORS_ORIGINS=http://localhost:3000,https://your-domain.com
# Comma-separated list of allowed origins for CORS

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
DEBUG_MODE=false
# Enable debug mode (automatically disabled in production)

AUTO_RELOAD=false  
# Enable auto-reload on file changes (development only)

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
APP_PORT=8000
# Port for the application container

NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
# Nginx proxy ports (if using docker-compose)

# =============================================================================
# EXAMPLE CONFIGURATIONS BY ENVIRONMENT
# =============================================================================

# Development Example:
# ENVIRONMENT=development
# LOG_LEVEL=DEBUG
# SUPABASE_URL=https://dev-project.supabase.co
# OPENAI_API_KEY=sk-dev...
# (Security settings optional)

# Staging Example:  
# ENVIRONMENT=staging
# LOG_LEVEL=INFO
# ADMIN_API_KEY=staging-admin-key-32-chars-min
# JWT_SECRET=staging-jwt-secret-48-chars-minimum-length
# SENTRY_DSN=https://...sentry.io/...

# Production Example:
# ENVIRONMENT=production
# LOG_LEVEL=INFO
# LOG_FILE=/app/logs/app.log
# ADMIN_API_KEY=prod-admin-key-super-secure-32-chars-min
# JWT_SECRET=production-jwt-secret-super-secure-48-chars-min
# SENTRY_DSN=https://...sentry.io/...
# REDIS_URL=redis://redis:6379
# RATE_LIMIT_ENABLED=true